import GlobalPage from '@/components/GlobalPage'
import { Box, Button } from 'grommet'
import React from 'react'

interface IPageProps {}

const Page: React.FC<IPageProps> = (props) => {
  return <GlobalPage>About</GlobalPage>
}

export default Page
